{
  "name": "autonomous-agent-marketplace",
  "private": true,
  "version": "1.0.0",
  "description": "Autonomous AI agent marketplace with on-chain verifiable execution.",
  "workspaces": [
    "contracts",
    "backend",
    "agents",
    "frontend"
  ],
  "scripts": {
    "dev": "node ./scripts/dev.mjs",
    "dev:node": "npm run dev:node --workspace contracts",
    "deploy": "npm run deploy --workspace contracts",
    "backend": "npm run dev --workspace backend",
    "agent": "npm run dev --workspace agents",
    "frontend": "npm run dev --workspace frontend",
    "test": "npm run test --workspace contracts",
    "build": "npm run build --workspace backend && npm run build --workspace agents && npm run build --workspace frontend"
  },
  "overrides": {
    "hardhat": "^2.28.0"
  }
}
